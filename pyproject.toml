[project]
name = "meyno"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "sqlalchemy>=2.0.0,<2.1.0", # Locking to 2.0.X verisons of sqlalchemy for now
]
